#!/usr/bin/env bash

for i in "$@" ; do
    cd ~/src/ledger
    work fix-${i} origin master
    ( cd fix-${i} ;                             \
      de ;                                      \
      direnv allow . ;                          \
      spawn "ai --model sonnet '/fix $i'")
done
