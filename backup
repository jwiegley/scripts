#!/bin/bash -x

push hermes
push mybook
push vault

function remote () {
    ssh hermes bash -l -c "$@"
}

remote zfs-snapshot daily mybook
remote zfs-snapshot day_of_week mybook
remote zfs-snapshot weekly mybook
remote zfs-snapshot monthly mybook
remote zfs-snapshot yearly mybook

remote b2-sync 400
