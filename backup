#!/bin/bash -x

push hermes
push tank
push vault
push mybook

function remote () {
    ssh hermes bash -l -c "$@"
}

remote zfs-snapshot daily tank
remote zfs-snapshot day_of_week tank
remote zfs-snapshot weekly tank
remote zfs-snapshot monthly tank
remote zfs-snapshot yearly tank

remote b2-sync 400
