#!/bin/sh
sudo rsync $1 -aXP --delete \
    --exclude=/.TemporaryItems/ \
    --exclude=/.Caches/ \
    --exclude=/.Trashes/ \
    --exclude=/Downloads/ \
    --exclude=/DVDs/ \
    --exclude=/Machines/ \
    --exclude=/Products/ \
    --exclude='/TV Shows/' \
    --exclude=/.fseventsd/ \
    --exclude=/.Spotlight-V100/ \
    --exclude=.DS_Store \
    --exclude='/Music/iTunes/iTunes Media/Podcasts/' \
    --exclude='/Music/iTunes/iTunes Media/iTunes U/' \
    /Volumes/RAID/ /Volumes/AA1T/
