#!/bin/bash

if [[ "$1" != --me && -d /Volumes/CEG ]]; then
    (cd /Volumes/CEG; bin/svn-mirror "$@")
fi

git find-fetch "$@" ~/Archives ~/src ~/bin ~/doc ~/Sites \
    ~/Library/Emacs ~/Library/Application\ Support ~/Library/Scripts

echo All repositories synchronized.
