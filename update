#!/bin/bash
set -euo pipefail
IFS=$'\n\t'

JOBS=10

(running fetchmail || getmail) &

(start-hackage ; diskutil eject /Volumes/Hackage) &

for dir in Contracts Projects; do
    (cd $dir; git each remote update)
done

wait
