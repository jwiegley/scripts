#!/bin/bash -e

if [[ "$1" == -a ]]; then
    (running fetchmail || getmail) &

    (start-hackage ; diskutil eject /Volumes/Hackage) &
fi

git each --dir "$HOME/Contracts $HOME/Projects" fetch --all -q

wait
