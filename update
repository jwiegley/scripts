#!/usr/bin/env bash

find "$@" -name .git -type d -print | sort | \
while read dir; do
    echo ===== $dir =====
    base=$(basename "$dir")
    (cd "$base"; git --git-dir="$dir" fetch --all --tags)
done
