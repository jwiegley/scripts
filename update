#!/bin/bash -e

if [[ "$1" == -a ]]; then
    (running fetchmail || getmail)
fi

update-nix -a -p --leq -Q -j12 -k

cd ~/emacs
make update-pdf-tools

git each --dir "$HOME/Contracts $HOME/Projects" fetch --all --no-tags -q

echo Updated completed
