#!/usr/bin/env bash

find "$@" -name .git -type d -print | sort | \
while read dir; do
    echo ===== $dir =====
    folder=$(dirname "$dir")
    (cd "$folder"; git --git-dir="$dir" fetch --all --tags)
done
