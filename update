#!/bin/bash

for dir in "$@"; do
    git each-verbose                                       \
        --dir "$dir"                                       \
        fetch --all --no-tags --no-recurse-submodules -q
done
